// HOF=> a funciton which take other function as an argument and return a function is called HOF.

// Built in HOF=>
// Map, foreach, filter and reduce (only for array)

// map function=>return element in the array of the same size.

// forEach=> return the value, not the value in the array.

// filter=>return value in the array which satisfies condition.

//reduce=> it summarize the total value of array and return that.






